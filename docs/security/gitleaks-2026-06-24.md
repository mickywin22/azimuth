# Gitleaks Secret Scan — Azimuth Public-Flip Gate (C1)

> **HARD GATE** for the W26 Azimuth public-flip. Run a secret scan over the
> working tree **and** full git history before the repository is made public.
> If any secret is found, the flip is **blocked**.

## Verdict

# ✅ CLEAN — gate PASSED. Public flip may proceed.

No secrets, keys, tokens, or credentials were detected in the Azimuth working
tree or in any reachable git history (publishable surface = `main` + the
unmerged OKF fleet branch headed for `main`).

## Scan metadata

| Field | Value |
|-------|-------|
| Tool | [gitleaks](https://github.com/gitleaks/gitleaks) |
| Tool version | `8.21.2` |
| Ruleset | gitleaks built-in default rules (no custom `.gitleaks.toml`, no allowlist) |
| Scan timestamp | 2026-06-24 13:49 (Europe/Berlin) |
| Repo | `C:\Users\Michael\projects\azimuth` |
| Base scanned | `origin/main` @ `deb52cd` + unmerged `fleet/azimuth-l1-flowback-W26-06230250` |
| Redaction | `--redact` enabled (any match would be redacted, never printed in clear) |

## What was scanned

Three independent passes — the publishable surface is `main` plus the one
unmerged fleet branch (`fleet/azimuth-l1-flowback-W26-06230250`) that carries
the OKF Tier-1 + flowback work destined for `main` before the flip.

| # | Pass | Command | Commits / scope | Findings | Exit |
|---|------|---------|-----------------|----------|------|
| 1 | Full git history (`main`) | `gitleaks git . --redact` | 28 commits scanned | **0** | 0 |
| 2 | Working tree (no-git dir) | `gitleaks dir . --redact` | entire working tree | **0** | 0 |
| 3 | Unmerged OKF branch history | `gitleaks git . --log-opts="origin/main..fleet/azimuth-l1-flowback-W26-06230250" --redact` | 5 extra commits | **0** | 0 |

All three passes returned `no leaks found` and exit code `0`. The machine-
readable JSON reports for each pass were empty arrays (`[]`).

## Interpretation

- **Working tree:** clean — no secrets staged or sitting unstaged on disk.
- **Git history:** clean — no secret was ever committed and later removed (the
  classic public-repo trap). History rewrite is therefore **not** required.
- **Unmerged content:** the OKF Tier-1 / flowback commits that will land on
  `main` before the flip are also clean.

## Gate decision

**PASS.** The Azimuth repository contains no detectable secrets in its working
tree or any reachable git history. This HARD gate (C1) is satisfied and the
public flip is **not** blocked by secret-exposure risk.

## Reproduce

```bash
# from the azimuth repo root, with gitleaks >= 8.21 on PATH:
gitleaks git . --redact -v                 # full history
gitleaks dir . --redact -v                 # working tree
gitleaks git . --redact \
  --log-opts="origin/main..<unmerged-branch>"   # any branch headed for main
# exit 0 = clean, exit 1 = leaks found
```

---
*Generated for the Azimuth C1 public-flip secret-scan gate. The gitleaks binary
used for this scan was downloaded to a gitignored, untracked `.tmp-tools/`
directory and is not part of the repository.*
