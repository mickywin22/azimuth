# L1 Sources

Machine-written verbatim transforms of the WorldMonitor API, one dated folder per daily
ingest run (`YYYY-MM-DD/<source_key>.md`). Written by `ingest/pull.py`; **never hand-edited**
— the synthesis lint `check_diff_guard` blocks any L2 commit that touches this lane.
